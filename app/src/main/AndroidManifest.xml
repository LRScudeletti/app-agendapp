<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lrssoftwares.com.br.agendapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PrincipalActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastroUsuarioActivity" />
        <activity android:name=".CalendarioEventosActivity" />
        <activity android:name=".CadastroEventosActivity" />
        <activity android:name=".AtualizacaoEventosActivity" />
        <activity android:name=".ConfiguracoesActivity" />
        <activity android:name=".ContatoActivity" />
        <activity android:name=".SobreActivity" />
        <activity android:name=".ConsultaEventosActivity" />
    </application>

</manifest>